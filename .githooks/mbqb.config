# Git 2.54+ declarative hooks (see `git help hook`). Enable with:
#   bash scripts/setup-githooks.sh
[hook "mbqb-pre-commit"]
	event = pre-commit
	command = bash .githooks/run-pre-commit.sh

[hook "mbqb-pre-push"]
	event = pre-push
	command = bash .githooks/run-pre-push.sh
